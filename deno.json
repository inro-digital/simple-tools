{
  "fmt": {
    "proseWrap": "preserve",
    "semiColons": false,
    "singleQuote": true
  },
  "tasks": {
    "dev": "deno run --watch main.ts",
    "test": "deno fmt && deno lint && deno task test:unit",
    "test:unit": "deno test --check=all ./",
    "test:update": "deno test -A -- --update source"
  }
}
